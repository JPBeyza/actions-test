name: Injection Test

on:
  issues:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Safely print issue title
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          echo "New issue title is: $ISSUE_TITLE"
