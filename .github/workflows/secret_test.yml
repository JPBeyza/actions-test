name: Secret Masker Test

on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Print Protected Secret
        run: |
          echo "Token value is: ${{ secrets.MY_SENSITIVE_TOKEN }}"

